services:
  - type: web
    name: crud-medialunas-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # - key: MONGO_URI
      #   value: your_mongo_connection_string
      # - key: EMAIL_USER
      #   value: your_email
      # - key: EMAIL_PASS
      #   value: your_email_password
